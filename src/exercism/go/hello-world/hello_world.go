/*
Package greeting implements a library to return Hello, World!

greeting doesn't accept any arguments and always does a constant greeting
*/
package greeting

// HelloWorld returns constant hello string whenever executed
func HelloWorld() string {
	return "Hello, World!"
}
